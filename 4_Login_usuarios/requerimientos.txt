#  pip freeze    (para ver librerías instaladas)
#
#  pip install -r requerimientos.txt > requerimientos.log        (instala dependencias, displays a fichero.log)

Flask
Flask-login
Flask-WTF
email-validator